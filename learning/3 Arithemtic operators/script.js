// arithemetic operatros = operands (values, variables, etc.) and operators (+, -, *, /, %)

let students = 30;

// students = students + 1;
// students = students - 1 ;  
// students = students * 2; 
// students = students / 2;
// students = students **2;

// students += 1; // students = students + 1
// students -= 1; // students = students - 1
// students *= 2; // students = students * 2
// students /= 2; // students = students / 2
// students **= 2; // students = students ** 2

students++; // students = students + 1
students--; // students = students - 1

console.log(students);

// operator precedence 
// 1. Parentheses ()
// 2. Exponents (**)
// 3. Multiplication (*), Division (/), Modulus (%)
// 4. Addition (+), Subtraction (-)

let result = 3 + 4 * 2; // 3 + (4 * 2) = 3 + 8 = 11
console.log(result);
